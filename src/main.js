import './style.css'
import AOS from 'aos'
import 'aos/dist/aos.css'

// Initialize Animate On Scroll
AOS.init({
    duration: 800,
    easing: 'ease-out-cubic',
    once: true,
    offset: 50,
});

console.log('Paixão Seguros Landing Page Initialized');
