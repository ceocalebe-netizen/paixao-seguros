# Paixão Seguros Landing Page

Projeto de Landing Page "Tech-Elegant" para Paixão Seguros, desenvolvido com Vite e Tailwind CSS.

## 🚀 Como Rodar o Projeto

Como a instalação automática do Node.js não foi possível, siga os passos abaixo para iniciar o projeto:

### 1. Pré-requisitos
Certifique-se de ter o **Node.js** instalado em seu computador.
Você pode baixar em: [nodejs.org](https://nodejs.org/)

### 2. Instalação
Abra o terminal na pasta do projeto e execute:

```bash
npm install
```

Isso instalará todas as dependências listadas no `package.json` (Tailwind, AOS, Phosphor Icons, etc).

### 3. Executando Localmente
Para iniciar o servidor de desenvolvimento:

```bash
npm run dev
```

O terminal mostrará um link (geralmente `http://localhost:5173`) para você acessar a página.

### 4. Build de Produção
Para gerar os arquivos otimizados para deploy:

```bash
npm run build
```
Os arquivos estarão na pasta `dist`.
